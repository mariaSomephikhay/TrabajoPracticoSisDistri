/**
 * Api Documentada
 * Documentación de la API de ejemplo con Flask + gRPC
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EventoRol from './EventoRol';

/**
 * The EventoUsuario model module.
 * @module model/EventoUsuario
 * @version 1.0
 */
class EventoUsuario {
    /**
     * Constructs a new <code>EventoUsuario</code>.
     * @alias module:model/EventoUsuario
     * @param username {String} 
     * @param email {String} 
     * @param nombre {String} 
     * @param apellido {String} 
     * @param activo {Boolean} 
     * @param rol {module:model/EventoRol} 
     */
    constructor(username, email, nombre, apellido, activo, rol) { 
        
        EventoUsuario.initialize(this, username, email, nombre, apellido, activo, rol);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username, email, nombre, apellido, activo, rol) { 
        obj['username'] = username;
        obj['email'] = email;
        obj['nombre'] = nombre;
        obj['apellido'] = apellido;
        obj['activo'] = activo;
        obj['rol'] = rol;
    }

    /**
     * Constructs a <code>EventoUsuario</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EventoUsuario} obj Optional instance to populate.
     * @return {module:model/EventoUsuario} The populated <code>EventoUsuario</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EventoUsuario();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('nombre')) {
                obj['nombre'] = ApiClient.convertToType(data['nombre'], 'String');
            }
            if (data.hasOwnProperty('apellido')) {
                obj['apellido'] = ApiClient.convertToType(data['apellido'], 'String');
            }
            if (data.hasOwnProperty('telefono')) {
                obj['telefono'] = ApiClient.convertToType(data['telefono'], 'String');
            }
            if (data.hasOwnProperty('activo')) {
                obj['activo'] = ApiClient.convertToType(data['activo'], 'Boolean');
            }
            if (data.hasOwnProperty('rol')) {
                obj['rol'] = EventoRol.constructFromObject(data['rol']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EventoUsuario</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EventoUsuario</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EventoUsuario.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['nombre'] && !(typeof data['nombre'] === 'string' || data['nombre'] instanceof String)) {
            throw new Error("Expected the field `nombre` to be a primitive type in the JSON string but got " + data['nombre']);
        }
        // ensure the json data is a string
        if (data['apellido'] && !(typeof data['apellido'] === 'string' || data['apellido'] instanceof String)) {
            throw new Error("Expected the field `apellido` to be a primitive type in the JSON string but got " + data['apellido']);
        }
        // ensure the json data is a string
        if (data['telefono'] && !(typeof data['telefono'] === 'string' || data['telefono'] instanceof String)) {
            throw new Error("Expected the field `telefono` to be a primitive type in the JSON string but got " + data['telefono']);
        }
        // validate the optional field `rol`
        if (data['rol']) { // data not null
          EventoRol.validateJSON(data['rol']);
        }

        return true;
    }


}

EventoUsuario.RequiredProperties = ["username", "email", "nombre", "apellido", "activo", "rol"];

/**
 * @member {String} id
 */
EventoUsuario.prototype['id'] = undefined;

/**
 * @member {String} username
 */
EventoUsuario.prototype['username'] = undefined;

/**
 * @member {String} password
 */
EventoUsuario.prototype['password'] = undefined;

/**
 * @member {String} email
 */
EventoUsuario.prototype['email'] = undefined;

/**
 * @member {String} nombre
 */
EventoUsuario.prototype['nombre'] = undefined;

/**
 * @member {String} apellido
 */
EventoUsuario.prototype['apellido'] = undefined;

/**
 * @member {String} telefono
 */
EventoUsuario.prototype['telefono'] = undefined;

/**
 * @member {Boolean} activo
 */
EventoUsuario.prototype['activo'] = undefined;

/**
 * @member {module:model/EventoRol} rol
 */
EventoUsuario.prototype['rol'] = undefined;






export default EventoUsuario;

